lightgbm==4.7.0
lime==0.2.0.1
mlflow==3.15.1
mlflow_skinny==3.15.1
mlflow_tracing==3.15.1
numpy==2.5.2
pandas==2.3.3
faiss-cpu==1.15.0  # features/faiss_index.py
# use conda install -c pytorch -c nvidia faiss-gpu     
# for faiss on gpu system, uninstall faiss cpu
scikit_learn==1.9.0
sentence-transformers==6.0.0  # features/text_embeddings.py - all-MiniLM-L6-v2
shap==0.52.0
jupyter
pytest
httpx  # serving/app.py test client (fastapi.testclient)
fastapi==0.141.1  # serving/app.py
uvicorn==0.52.4    # serving/app.py - run with `uvicorn serving.app:app`
feast==0.66.0
# dill version pin is required, not cosmetic: feast declares dill~=0.3.0
# and pip will warn about this pin conflicting with that, but the
# installed 0.3.9 cannot serialize the on-demand feature view UDF in
# feature_repo/definitions.py under Python 3.14 - `feast apply` fails
# with `TypeError: _Pickler._batch_setitems() missing 1 required
# positional argument: 'obj'` (dill's pickler internals assume a
# pre-3.14 cpython pickle.Pickler signature). 0.4.1 fixes this; keep the
# pin until feast's own requirement catches up.
dill==0.4.1
torch

# use conda install -c pytorch -c nvidia faiss-gpu      for faiss on gpu system, uninstall faiss cpu